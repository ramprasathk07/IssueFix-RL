# torch is pre-installed on Kaggle — install separately if running locally:
#   pip install torch>=2.1.0

# Core
transformers>=4.40.0
pydantic>=2.0.0
pyyaml>=6.0

# Training
bitsandbytes>=0.43.0
peft>=0.10.0
tqdm>=4.66.0
accelerate>=0.27.0

# Logging & experiment tracking (optional)
wandb>=0.17.0
mlflow>=2.13.0
safetensors>=0.4.0

# Testing
pytest>=8.0.0
